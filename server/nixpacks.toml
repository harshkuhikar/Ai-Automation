# Force Node.js detection with Python support
[phases.setup]
nixPkgs = ["nodejs", "python311", "python311Packages.pip", "python311Packages.requests", "python311Packages.python-dotenv"]

[phases.install]
cmds = ["npm ci || npm install"]

[start]
cmd = "node server.js"
